{
  "name": "apicompass",
  "version": "1.0.0",
  "description": "Private in-browser JSON formatter, validator, and payload inspector.",
  "private": true,
  "main": "desktop/main.cjs",
  "scripts": {
    "dist": "electron-builder --win portable",
    "check": "node --check app.js"
  },
  "devDependencies": {
    "electron": "^36.3.2",
    "electron-builder": "^26.0.12",
    "@capacitor/android": "^8.5.2",
    "@capacitor/cli": "^7.4.0",
    "@capacitor/core": "^7.4.0"
  },
  "build": {
    "appId": "dev.yuin.apicompass",
    "productName": "API Compass",
    "directories": {
      "output": "release"
    },
    "files": [
      "index.html",
      "styles.css",
      "app.js",
      "desktop/**/*",
      "package.json"
    ],
    "win": {
      "target": "portable",
      "artifactName": "API Compass-Portable-${version}.exe"
    }
  }
}
