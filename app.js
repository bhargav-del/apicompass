const $=s=>document.querySelector(s);const input=$('#input');const sample=`{\n  "orderId": "ord_8821",\n  "customer": "Northstar Labs",\n  "items": [{"sku":"starter","qty":2}],\n  "total": 1280,\n  "currency": "USD",\n  "paid": false\n}`;function toast(t){$('#toast').textContent=t;$('#toast').classList.add('show');setTimeout(()=>$('#toast').classList.remove('show'),2600)}function depth(v){if(v===null||typeof v!=='object')return 0;const vals=Array.isArray(v)?v:Object.values(v);return vals.length?1+Math.max(...vals.map(depth)):1}function escape(t){return t.replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]))}function update(mode='pretty'){try{const data=JSON.parse(input.value);const pretty=JSON.stringify(data,null,mode==='pretty'?2:0);if(mode==='pretty')input.value=pretty;const keys=data&&typeof data==='object'&&!Array.isArray(data)?Object.keys(data).length:0;const d=depth(data);const chars=input.value.length;$('#hero-status').textContent='Payload is valid';$('#hero-sub').textContent='Your data is parsed locally and ready to ship.';$('#validation').textContent='✓ Valid JSON';$('#result-pill').textContent='Valid';$('#key-count').textContent=keys;$('#size').textContent=`${new Blob([input.value]).size} B`;$('#metric-keys').textContent=keys;$('#metric-depth').textContent=d;$('#metric-lines').textContent=input.value.split('\n').length;$('#r-keys').textContent=keys;$('#r-depth').textContent=`${d} levels`;$('#r-chars').textContent=chars;$('#preview').innerHTML=`<pre>${escape(pretty).slice(0,850)}</pre>`;input.classList.remove('invalid')}catch(err){$('#hero-status').textContent='Payload needs attention';$('#hero-sub').textContent=err.message;$('#validation').textContent='× Invalid JSON';$('#result-pill').textContent='Invalid';$('#result-pill').className='pill red';input.classList.add('invalid')}}$('#format').onclick=()=>{update('pretty');toast('JSON formatted.')} ;$('#minify').onclick=()=>{update('minify');toast('JSON minified.')} ;$('#sample').onclick=()=>{input.value=sample;update('pretty');toast('Sample payload loaded.')} ;$('#copy').onclick=async()=>{await navigator.clipboard?.writeText(input.value);toast('Formatted JSON copied.')} ;$('#copy-top').onclick=()=>$('#copy').click();input.oninput=()=>update('none');document.addEventListener('keydown',e=>{if((e.metaKey||e.ctrlKey)&&e.key==='Enter'){e.preventDefault();update('pretty')}});update('pretty');